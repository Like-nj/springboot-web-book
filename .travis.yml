language: java
jdk:
  - openjdk8

branches:
  only:
    - master

before_install:
  - chmod +x gradlew

#Travis CI server home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r nj-webservice *
  - mkdir -p deploy
  - mv nj-webservice.zip deploy/nj-webservice.zip # deploy로 zip파일 이동

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    bucket: nj-webserver-build # s3 버킷
    region: ap-northeast-2
    skip_cleanup: true
    acl: private # zip 파일 접근은 private로
    local_dir: deploy # before_deploy에서 생성한 디렉토리
    wait_until_deployed: true

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    bucket: nj-webserver-build # s3 버킷
    key: nj-webservice.zip
    bundle_type: zip
    application: nj-webserver-cd
    deployment_group: nj-webserver-cd-group
    region: ap-northeast-2
    wait_until_deployed: true

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - izima88@naver.com